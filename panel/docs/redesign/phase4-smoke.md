# 面板重构 · 第 4 阶段 · 联调冒烟

> 协调会话写于 2026-09-25。worktree `../pandora-smoke`，分支 `feat/panel-redesign-smoke`，不写迁移。
> 先读 `phase3-common.md`（第 1–11 节，尤其第 10.3 节），再读本文件。

## 目标

新前端到现在只对假后端验过。第 10.3 节说过：zod schema 是前端和后端对账的唯一防线。本会话在 CI 上起真实的 PG18 + 网关，**用前端页面自己的 zod schema** 去解析真网关的响应。这样查出来的就是「页面拿到真数据会不会崩」，而不是另写一套断言去比较。用户定的顺序是：先用 CI 冒烟挡住接口形状不对的问题，再上测试机人工点。

## 步骤（每步一个提交，做完停下报告）

| 步 | 内容 |
|---|---|
| ① 起栈 | 在 CI 上加一个任务（放进 `panel-pg18.yml`，或者新建 workflow，二选一并说明理由）：PG18 容器 → goose 迁移 → 用 `aegis-adminctl` 建管理员 → 用 CI 里临时生成的配置（不含任何真实值，第 11 条铁律）从源码启动 `aegis-public` 与 `aegis-admin` → 健康检查。复用 `deploy/run-pg18-gates.sh` 与 `tests/*_e2e.sh` 里现成的做法，不另起炉灶。触发路径包括 `panel/internal`、`panel/cmd`、`panel/migrations`、`panel/frontend/src` 和冒烟文件本身 |
| ② 造数据 | 跑一条最小业务链，让各列表都不为空（空数组对 schema 几乎什么都验不出来）：注册与登录门户用户；后台建节点池、登记一个节点并上报心跳（照 `node_e2e.sh` / `uniproxy_e2e.sh`）；向导建套餐并发布（`AEGIS_SALES_ENABLED=1`）；门户下单并用演示渠道支付；工单、公告、知识库、礼品卡、优惠券各造一条 |
| ③ 形状校验 | 在 `panel/frontend/tests/smoke/` 下用单独的 vitest 配置（不进 `make frontend-check`）：以管理员和门户用户两个身份，对前端调用的**每个 GET 接口**，用页面实际使用的那个 schema 解析真实响应。接口清单从 `src` 里的调用处整理出来，写成表，每行标「已验 / 跳过及原因」 |
| ④ 写接口 | 抽样验证写路径：reauth 流程（`reauth_required` → reauth → 重放）、幂等（2xx 同 key 重放、换请求体 409）、每个模块至少一个写操作的响应能被页面 schema 解析 |

## 规则

- `src/` 里只允许一种改动：给页面内没导出的 schema 加 `export`，不改形状、不改页面逻辑。其余一律只动 `tests/smoke/`、CI 与部署脚本。
- **发现不一致不要修**：Go、页面、schema 都不改。把「接口、schema 期望、真实响应片段、判断是后端错还是前端错（按契约）」写进报告，协调会话写成修订、指派给后端三 / 后端四 / 前端收尾。
- 冒烟里不得出现真实服务器地址、域名、密钥（公开仓库，`.githooks` 的 gitleaks 会扫）。
- 后端三、后端四会改接口形状（R99–R104），形状以主线为准：做事前 `git merge feat/panel-redesign`，合进来的新字段让冒烟跟着变。

## 进度与补充事项（协调会话维护，接力的新会话从这里接上）

**进度**：①–⑤ 与 ⑥-1（合并 c377f0e）、⑥-2 `bac4347` + `8636239`（合并 d0a534a；panel-smoke 36205189238：e2e 六个脚本五个通过）已验收合入。**⑥-3 等后端四 ⑦（退役 aegis-agent 与 node_e2e.sh）合入主线后再做**，见补充事项。

补充事项（与上文冲突时以这里为准）：
- 契约修订已到 R116。
- ⑥-2 的验收结论：uniproxy 补幂等键、清理不再吞 stderr；e2e.sh 按默认关闭验证注册、验证码段经后台打开再恢复（带 EXIT 兜底）、错误验证码按契约统一 403、脚本自建带权益的套餐，都认可，都是脚本过时。R115 断言改成「code 为 ticket.replied 且正文含「工单标题」」是对的：协调会话写的「subject 就是工单标题」不准确，R115 原文说的是模板变量 `subject`。
- node_e2e 的处理（用户 2026-09-25 定）：**退役 aegis-agent**，删源码、出发布包、删 node_e2e.sh，交后端四 ⑦（会顺带删 `run-smoke-e2e.sh` 里编译 aegis-agent 的准备行）。它合入主线后你再做 **⑥-3**：`git merge feat/panel-redesign`，确认 e2e 表剩五个脚本，改成失败即让 job 变红，推送看 CI，报告。在那之前先停着。
- **⑥ 要做的（2026-09-25 定）**，按顺序：
  1. **先翻 R115 断言（急）**：后端四 ⑤ `8026cd0` 已验收，但还没合入主线，因为它会让你 `writes.smoke.ts` 里「通知没有 `ticket.replied`」那行变红。做法：`git merge origin/feat/panel-redesign-be4`（只带进这一个提交），把那行改成正向断言：等门户通知列表出现 `ticket.replied` 且 `subject` 是那张工单的标题；**必须带等待**：admin 网关不派发，站内信要等 public 网关的派发循环，最慢约 5 分钟（R115 补），超时就算失败。单独一个提交，推送，三组 CI 绿了就先报一句，协调会话随即按「后端四 ⑤ → 冒烟」合入主线。
  2. **修三个过时的 e2e 脚本**（只改 `panel/tests/` 下的脚本，不改 Go）：`node_e2e.sh` 签引导令牌、`uniproxy_e2e.sh` 签服务端令牌都补幂等键（照 `admin_e2e.sh` 的写法），uniproxy 的清理 SQL 不再吞 stderr；`e2e.sh` 的注册按现在的默认（邮箱验证关闭）走，要测验证码那一段就先经后台接口打开 `auth.email_verification` 再测、测完恢复；订单快照那条的前置数据由脚本自己造（套餐带权益），不依赖 seed-demo。每处在报告里写「改了什么、为什么是脚本过时而不是后端错」。如果修的时候发现真是后端的问题，停下报告，不改 Go。
  3. 六个脚本都通过后，把 e2e 表从「只报告」改成**失败即让 job 变红**，免得它们再次悄悄过时。
  可以分几个提交，第 1 件先单独报，2、3 做完再报。
- ⑤ 的验收结论：自定义 sequencer 固定「admin → portal → writes」（约束放在配置里，本机单跑也成立），认可；e2e runner 只在 Actions 上跑、脚本一字未改、为满足守卫把冒烟库改名 `aegis_smoke_test`、容器名 `aegis-postgres`，认可；三个失败的定性（node / uniproxy 缺幂等键、e2e.sh 注册前提过时与套餐无权益）核对无误，都是脚本过时，交你在 ⑥ 修。
- **⑤ 要做的（2026-09-25 定，新会话接）**：先 `git merge feat/panel-redesign`。
  1. **固定执行顺序**：写路径必须在读表之后跑（现在 vitest 不保证文件顺序）。用 vitest 的 sequence 配置或在 workflow 里分两次调用，二选一说明理由。
  2. **跑现有 e2e 脚本**：在冒烟栈上逐个跑 `panel/tests/*_e2e.sh`，只报告：每个脚本通过 / 失败、失败在哪一步、原因（过时的请求形状、缺幂等键、前置数据不对等），**不修脚本也不修 Go**。结果写进 job summary 另一张表，失败不让整个 job 红（用 continue-on-error 或脚本内收集退出码）。
  3. **工单回复通知**：后端四 ⑤ 在做 R115（非内部回复给提单人排 `ticket.replied`）。它合入主线后，把 ④ 那行「工单回复不发通知」改成断言门户通知列表里出现 `ticket.replied`；它还没合入就先不动，报告里注明。
- ④ 的验收结论：接入走真实的 Ed25519 两段式、上线后不带 warnings、以节点身份取用户并与门户订阅配置里的凭据对上；种子顺序「草稿版本绑池 → 节点上线 → 发布」；各空列表的造法与「SQL 夹具」标注（提现一条，理由成立）；插件钩子靠 test 环境本来就放行回环，没改 Go；重认证用 CI 现场生成的密钥把 `rat` 回拨 16 分钟重签（令牌格式一改这行会红，这正是想要的提醒，认可，不改成空等 15 分钟）；幂等三种情况；八个模块的写操作用页面自己的请求体构造与响应 schema，都认可。「条数」对详情接口只供参考，知道了。
- 「工单回复不发通知」经核实是后端缺陷（模板早已种下、代码从不排队），已写成 R115 指派给后端四 ⑤。
- 后端三 ⑤ 已合入主线（合并 bd0b7ff，R114）：工单详情计数、`paid_totals`、订单行 `manual`、变更试算券面、门户佣金 `scope`、会话 `last_seen_at` 都有了。前端收尾 ⑤ 会把这些字段在 schema 里改成必填；你 ④ 合主线时如果它已合入，逐行表会自动覆盖到，不用另加行。
- ③ 的验收结论：`src` 只加 `export` 与 L3 头注、单独的 vitest 配置、逐行表进 job summary、三个提交都只修冒烟脚本，都认可。「0 不一致」要打的两个折扣（空列表只验到外层、页面 schema 宽松不报多余字段）写得清楚，多余字段不追：宽松是有意的，冒烟只管页面拿到真数据会不会崩。
- 「支付通知疑似重复入队」已查（FACT）：`notification_deliveries` 有 `(tenant_id, dedupe_key)` 唯一约束，`Enqueue` 是 `ON CONFLICT DO NOTHING`，键是 `order-paid:<订单>:<渠道>`。第二轮扫描只是日志把「扫到的订单数」当成了「排队条数」，没有重复入队，也没有第二次派发。日志计数已交给后端三 ⑤，你不用管。
- **④ 要做的**（可以拆成几个提交，全部做完再报告）：
  1. **先上线接口**：`git merge feat/panel-redesign`（带进后端四 ④ 与你的 ③ 合并），`seed.ts` 改用 `POST v1/nodes/{id}/activate` 一步上线，删掉逐级调旧 `POST v1/nodes/{id}/status` 的那段；逐行表加一项「新服务器 + 新节点一步上线后能下发用户」：以节点身份取用户列表，断言门户用户在里面。
  2. **扩种子数据**：你列的空列表各造至少一条，逐行表加一列「条数」，把「验到行」和「只验到外层」分开标。优先走产品接口；在线设备、流量排行、IP 聚类这类只能由节点上报或后台任务产生的，先用节点上报造；实在造不出来的，可以像演示支付渠道那样直接写 SQL 夹具，逐行表里标「SQL 夹具」。每一行都要说明为什么这么造。
  3. **插件钩子投递**：只有在不改 Go、不放宽出站地址校验的前提下，冒烟里起一个本机接收端能收到投递，才去造；收不到就继续跳过，写明被什么挡住。
  4. **写路径**：按步骤表 ④（reauth 重放、幂等重放与换请求体 409、每个模块至少一个写操作的响应能被页面 schema 解析）。顺带核实「工单回复发不发通知」：后台回复一条工单，看门户通知列表，结论写成 FACT。
- 前端收尾 ③ ④ 与上线补丁已合入主线（合并 873a6f1），`users/api.ts` 的冲突协调会话已保留双方，冒烟的 tsconfig 在合并后类型检查通过。④ 第 1 步合主线会一并带进来：上线接口的页面 schema 现在就是 `adminNodeSchema`（`warnings` 可缺省、出现就至少一条），写路径抽样可以直接用它。
- **上线接口已合入主线（后端四 ④，合并 b8f8520，R113）**：③ 报告交完后（或并进 ③ 的补丁），`seed.ts` 改用 `POST v1/nodes/{id}/activate` 一步上线，删掉逐级调旧 `POST v1/nodes/{id}/status` 的那段，并把「新服务器 + 新节点一步上线后能下发用户」作为冒烟的一项。
- ③ 预审（协调会话看了本地提交）：`src` 里只给 13 个 schema 加了 `export`，认可；`tsconfig.node.json` 排除 `tests/smoke`、冒烟用自己的 tsconfig，改动面最小，认可。推不上去时不改走 HTTPS 或令牌是对的。推上去后在报告里附 job summary 里的逐行结果（已验 / 跳过 / 不一致），不一致的逐条写「接口、schema 期望、真实响应片段、按契约判断是谁错」。
- 前端收尾 ④ 会把用户、内容两处套餐查询的键挪到 `['admin','plans','options',<模块>]`（schema 不变），可能和你给 `users/api.ts` 加 export 的那几行冲突，合并时保留双方。
- ② 的验收结论：起栈加 aegis-node 只探 healthz、`seed.ts` 只用 Node 内置模块不需要 npm ci、后台请求隔 300ms、每步验状态码并带响应片段退出、口令随机生成且不打印、最后逐个查列表条数，都认可。直接写 SQL 插演示支付渠道（照 `deploy/seed-demo.sql`，加 CNY）作为冒烟夹具认可，只能出现在冒烟里。
- 节点上线：已定新接口 `POST v1/nodes/{id}/activate`（R108，后端四 ④），合入前继续用旧接口逐级推进，合入后改用它（见上一条）。
- 通知列表：扫描间隔写死在 `cmd/aegis-public/main.go`（启动约 30 秒后第一次，之后每 5 分钟），③ 按你说的「等到非空或超时就标跳过」，跳过的写进清单与原因。不要为此改 Go。
- 「工单回复不发通知」没核实，③ 不要依赖它，写成 UNKNOWN。
- **新增可选第 ⑤ 步**（④ 之后）：在冒烟栈上跑仓库现有的 `tests/*_e2e.sh`（本机没有数据库，它们平时从没人跑，已有迹象过时，例如 `uniproxy_e2e.sh` 签服务端令牌没带幂等键）。跑不通的只报告原因，不修脚本也不修 Go，协调会话再指派。
- 节点上线：新前端现在上不了线，协调会话定了新接口 `POST v1/nodes/{id}/activate`（R108，后端四 ④）。在它合入主线前，冒烟继续用旧的 `POST v1/nodes/{id}/status` 逐级推进；合入后改用新接口，并把「新服务器 + 新节点一步上线后能下发用户」作为冒烟的一项。
- ① 的验收结论：单独的 `panel-smoke.yml`（触发面与失败含义不同）、`run-smoke-stack.sh up/down`、配置全用 openssl 现场生成、不读 secrets、认证限流放宽只在冒烟栈里，都认可。一步两个提交（第二个修 workflow 语法）可以接受。
- ② 要节点上报心跳：在同一个脚本里加起 `aegis-node` 网关，认可。
- 后台 IP 限流每分钟 240 次写死在代码里：在冒烟脚本里控制请求节奏，不改 Go，认可。
- 后端四 ① 已合入：**没划进节点池的节点不服务任何用户**（R104、R105）。② 造数据时节点必须先划进池、池再绑到套餐版本上，否则用户列表为空。
