# 面板重构 · 第 3 阶段 · 门户前端（01–10）

> 协调会话写于 2026-09-24。worktree `../pandora-fe-portal`，分支 `feat/panel-redesign-fe-portal`，开发服务器端口 **5232**（`npm run dev:portal -- --port 5232 --strictPort`）。
> 先读 `phase3-common.md`（第 3、5、8、9 节与**第 10 节前端规则**），再读本文件。你看不到协调会话的项目记忆，决策以这几份文件和 `api-contract.md` 为准。

## 负责范围

- 门户 11 个页面（`pages.ts` 的 PageKey，含结账页 checkout）与外框里的支付弹窗。
- 目录：`src/portal/screens/<页面>/`、`dev/mock/portal/<页面>.ts`。
- 门户外框（`src/portal/` 下的 Shell、AuthPage、queries.ts 等）的后续改动归你。
- 开工前提：后台前端二的第 ⓪ 步「接线」已合入主线（页面登记表、子路由、拆分后的假后端）。开工先 `git merge feat/panel-redesign`，读 `src/portal/screens/CLAUDE.md` 与 `dev/mock/CLAUDE.md`。
- 门户在 < 640 时是手机布局：底部标签栏、弹窗变底部抽屉。每一步都要在 375 宽度下实测。

## 步骤（每步一个提交，做完停下报告）

| 步 | 内容 | 依据 |
|---|---|---|
| ① 概览与我的订阅 | 概览（问候、当前套餐、用量、到期、重置日、在线设备、公告）；我的订阅（订阅地址复制与换发、客户端导入、可用节点——**用户看不到节点国家和负载**、按日流量图、订阅拉取统计） | 门户-01、门户-02；保留规则 3 |
| ② 选购套餐与结账 | 套餐列表（按时间 / 流量包两类）、结账页（优惠券、余额抵扣 `use_balance`、续费遇改价、**变更套餐**的补差与退余额展示）、支付弹窗（epay 收银台跳转，无二维码，订单 30 分钟有效；`location.assign(redirect_url)` 顶层 GET 导航，`http_method=="POST"` 按「该支付方式暂不可用」处理，不渲染自动提交表单；回跳后订单详情轮询，细节全在契约门户-03 支付条目） | 门户-03、门户外壳与认证中的支付弹窗；5.A D-E-1、D-E-2；待决 D-E-3 |
| ③ 订单与钱包 | 订单列表与订单明细；钱包（余额、余额流水、充值、礼品卡兑换——流量奖励进流量包余额） | 门户-04、门户-05；修订 R31 |
| ④ 邀请返利 | 邀请链接（`/?invite=`）、被邀请人、佣金（**以账本为准**：账本余额 − 在途提现）、转余额、提现申请 | 门户-06；5.A D-F-1 |
| ⑤ 工单与消息 | 工单列表、新建、对话、关闭（关闭原因）、关联订单；消息列表、已读、铃铛角标（无 SSE，外框已 60 秒轮询） | 门户-07、门户-08；待决 D-F-2 |
| ⑥ 帮助与账号安全 | 帮助中心（知识库、「有帮助」反馈）；账号安全（改密码、会话列表与吊销、Telegram 绑定 `/start CODE`、通知偏好 transactional/service/marketing × email/telegram、快捷登录链接生成——**只能在已登录设备生成，60 秒有效**） | 门户-09、门户-10；保留规则 1；待决 D-F-3 |

后端依赖（写报告时核对主线进度）：
- ① 的按日流量接口在后端一 ⑤（M4）。
- ② 的流量包已实现（R30–R33）；变更套餐在后端一 ④；结账字段在后端一 ⑥。
- ③④ 的字段扩展在后端一 ⑥。
- ⑤ 的工单、消息扩展与 ⑥ 的「有帮助」反馈（M5）、me / 账号安全扩展在后端二 ④⑤。

## 待决项（按「未决前」处理，碰到时在报告里列出）

D-E-3、D-F-2、D-F-3。

## 进度与补充事项（协调会话维护，接力的新会话从这里接上）

**进度**：① 概览与我的订阅 `52e7726`（合并 676694c）、② 选购套餐与结账 `ea55742`（合并 860c3e3）、③ 订单与钱包 `cd690c4` + `0b0543d`（合并 8144a5d）、④ 邀请返利 `5316e05`（合并 d474f01）、⑤ 工单与消息 `44edd7e`（合并 fd3ecc3；NativeCore 36088330834 全绿）已验收合入。⑥ 帮助与账号安全 `679acfb`（合并 0c9945b；NativeCore 36099069077 全绿）已验收合入。**门户 ①–⑥ 全部完成，本会话不再开工。** 第 3 阶段收尾的真实联调发现问题时，协调会话会在同一 worktree 另开会话处理。

补充事项（与上文冲突时以这里为准）：
- 契约修订已到 R97。
- 外框已有的读接口（site-config、appearance、me、余额、当前套餐名、可用佣金、未读数）在 `src/portal/queries.ts`，页面要复用同一个查询键，不要重复定义。
- 外框顶栏读的 `me/balance`、`me/subscriptions`、`me/commission`、`me/notifications` 与快捷登录签发的假接口，⓪ 已搬进 `dev/mock/portal/` 下对应页面的文件（wallet / subs / referral / messages / account），扩充时就在那里改，形状不能破坏外框已有的读取。
- 字节、日期时间、计数的格式化函数（`formatBytes` 等）正由后台前端一提升到 `src/core/format.ts`，合入主线后协调会话通知你；在那之前如果需要，先在自己目录里临时写，合入后改为从 core 引用、删掉临时版本，不要各写一份长期留着。
- 后端二已全部完成（R53–R62）：门户改密保留当前会话（R62）；会话列表的 `last_seen_at` 没有实现，先不显示这一列；`GET v1/payment-methods` 已实现（R61）；工单 `related_order` 无关联时为 null（R60）。
- 后端一 ⑥ 第一部分已上线（R68、R69）：套餐目录重置策略与续费 / 变更开关、优惠码试算支持 `{ pack_id }` 并回券面、订单 `counts` 与明细字段、佣金概况 `paid_invitees` / `total_earned` / `transfers`、礼品卡预览不再回发行量、兑换记录 `code_hint`。
- ① 的验收结论：
  - **授权你改 `src/portal/queries.ts`**（门户外框归你）：订阅 schema 写全，外框经 `select` 取自己要的字段，页面与外框共用 `['portal','subscriptions']` 一个查询键，去掉 `detail` 子键与重复请求。放进 ② 的提交里。
  - 字节格式化改用 `core/format.ts` 的 `formatBytes` 等（后台前端一已提升，合并 3b0930e）；`screens/common/traffic.ts` 里与之重复的换算删掉，按切日时区出日期的函数先留在门户。
  - `published_at` 可为 null 已写成契约 R71。v2rayN 改为复制地址、critical 横幅与宽限期徽标，认可。
  - 你报告里标「尚未实现」的后端项现在都已上线：订阅扩展字段与 R50 时区口径（后端二 ⑤，R53–R62）、订单 `interval` / `item_name` / `counts`（后端一 ⑥，R69）。假后端 legacy 场景可以留着测降级，default 场景对齐最终形状。
- **ui 新组件已合入主线（后台前端二 1b1ac26，合并 c93dec6）**：`StatStrip`（四格统计条）、`Pager`（total / limit / offset 分页）、`QueryView`（react-query 结果的加载 / 404 无权限 / 错误重试 / 空 / 正文五态），从 `ui/index.ts` 取。本步新写的页面改用它们，自己目录里的临时版本删掉；`enabled: false` 的查询不要套 `QueryView`（`isPending` 会一直为真）。`Checkbox` 带文字标签时现在用 `htmlFor` 关联，按名字查找能找到。
- ② 的验收结论：
  - 余额改为「使用余额抵扣」开关、POST 跳转渠道提示暂不可用（epay 与 demo 实际都是 GET，契约 8.5 已核）、按请求指纹复用幂等键、头像菜单徽标改取到期最晚的生效订阅、`compactBytes`、D-E-3 降级，都认可。
  - 变更套餐试算不回券面已写成契约 R76，前端显示优惠金额即可。
  - 订单页地址统一用订单 id：`#/orders/<id>`（与支付回跳 return_url 一致），③ 照此实现。
  - 假后端的 `v1/__mock/cashier`、`v1/__mock/portal-scenario` 只在 dev 里，产物扫描已确认不进产物。
  - **② 没测 960**：③ 开工时先在 960 下把选购页和结账页补看一遍，有问题并进 ③ 的提交；以后每步三个宽度都要看。
  - ③ 起用 ui 的 `StatStrip` / `Pager` / `QueryView`，不合适的场景（如同时等多个查询）可以不套，报告里说明。
- ③ 的验收结论：
  - 订单页（待支付卡片、筛选计数、按月分组、「显示更早」、`#/orders/<id>` 展开与单独成卡）、支付弹窗新增「选择支付方式」一步、钱包（充值 ¥1–¥50000、余额明细、挂账显示「挂账转入」、礼品卡先查询后兑换、手机上礼品卡排在明细前）、删掉 USDT 与演示提示、`common/intent.ts` 共用幂等键，都认可。② 的 960 补看无问题。
  - **要改（并进 ④ 的提交）**：兑换响应的 `summary` 和 `GET v1/me/gift-cards` 的 `redemptions` 在 schema 里写成了可空，不对。协调会话核过 Go：`MyRedemptions` 用 `[]MyRedemption{}` 初始化，没记录时回 `[]`；`Redeem` 在 `summary` 为空时直接报错，成功时一定非空。契约写的 `string[]`、`[...]` 就是事实，两处改回非空数组（第 10.3 节：schema 写严，不为可能性放宽）。以后判断「Go nil 切片会编码成 null」之前，先看它是不是已经初始化成空切片。
  - 只改前端的推送不触发 PG18，报告只写 NativeCore（已写进公共规则 10.6）。
- **④ 邀请返利要点**：佣金以账本为准（5.A D-F-1）；分销概况的 `paid_invitees`、`total_earned`、`transfers` 已上线（R69）；「首单」文案取决于计佣范围 `scope`（R67，first_order 才能写「首单」）；转余额与提现申请都要幂等键（提现 R5），错误文案照后端。邀请链接格式 `/?invite=<code>`。
- ④ 的验收结论：
  - 邀请返利页、佣金 schema 收进 `['portal','commission']` 一个查询键、礼品卡两处 schema 改回非空数组、假后端按 Go `RequestWithdrawal` 的检查顺序与文案，都认可。
  - 和设计稿的三处出入认可：提现说明写「审核通过后打款」（后端没有时效承诺）；「邀请记录」卡宽屏放左列、手机上排在佣金记录之后；邀请码有上限时显示用量、用满或站点关闭注册时警示。
  - 契约：门户佣金概况不回 `scope`，横幅不写「首单」（R81）；转余额 409 显示后端原文并重拉、提现条目幂等以 R5 为准（R82）。
  - **幂等键 `reset()` 统一口径（协调会话定）**：所有写操作在成功或收到 4xx 业务拒绝后都调用 `reset()`，断网与 5xx 保留键以便重试回放。结账、充值、兑换礼品卡三处在 ⑤ 的提交里顺带改成同一口径，补单测。
- ui `Table` 已支持行级键盘激活（后台前端一 b1b9b77，合并 0e4cbd5）：传了 `onRowClick` 的行可 Tab 聚焦，Enter / 空格触发同一回调，不用再在单元格里放链接给键盘兜底。
- ⑤ 的验收结论：
  - 工单页（地址决定布局、窄屏分屏、撤回 / 关闭 / 回复、从订单页预填关联订单）、消息页（通知跳转、全部已读、公告级别共用 `SEVERITY_LABEL`）、假后端照 Go `domain/support`，都认可。
  - 和设计稿的三处出入认可：副标题不承诺「10 分钟内首次响应」；「问题已解决」加确认框；新建表单加「关联订单（可选）」、正文至少 10 字。D-F-2 客服统一显示「客服」按「未决前」。
  - 幂等键 reset 口径与 `usePlacedOrder` 认可（协调会话定口径时没考虑到下单类动作，你补得对）。**要补（并进 ⑥ 的提交）**：现在没有任何地方调用 `forget()`，刚下的待支付单在 30 分钟内被取消、超时或已支付后，同样的请求再点仍会重开这张单、支付必然失败。改为：重开时支付接口回 4xx（订单已不可支付）就 `forget()` 并按新请求下单，或在订单状态变为非待支付时 `forget()`；补单测。
  - 契约：站内信 `sent_at` 可为 null（R83）；单条标已读非 UUID 回 500 是后端缺陷（R84，列入遗留）。
- ⑥ 的验收结论：帮助中心（地址决定布局、按分类分组、`## ` 小标题只渲染纯文本、反馈与读正文参数一致 R43）、账号安全（改密 401 内联不登出、快捷登录只在已登录设备生成并 60 秒倒数、会话按 D-F-3 不显示城市与 IP、Telegram 四态与轮询、偏好先改后回滚）、`forget()`（重开前先查订单是否可付、支付 409 也 forget），都认可。和设计稿的三处出入认可：会话下线不加确认、只有「有帮助」、偏好合并成 3 类。契约：R88、R89。
- 留给以后（不急，假后端简化）：会话吊销不让外壳发的令牌失效（要开放 `dev/mock-api.ts` 的会话表）；快捷登录重新生成时旧令牌不作废（`dev/mock/quick-login.ts`，Go 会作废）。
