# 面板重构 · 第 3 阶段 · 后台前端一：仪表盘、工单、用户、套餐、订单与收款（01–05）

> 协调会话写于 2026-09-24。worktree `../pandora-fe-admin1`，分支 `feat/panel-redesign-fe-admin1`，开发服务器端口 **5211**（`npm run dev:admin -- --port 5211 --strictPort`）。
> 先读 `phase3-common.md`（第 3、5、8、9 节与**第 10 节前端规则**），再读本文件。你看不到协调会话的项目记忆，决策以这几份文件和 `api-contract.md` 为准。

## 负责范围

- 后台模块：`dash`（后台-01 仪表盘）、`tickets`（后台-02 工单）、`users`（后台-03 用户）、`plans`（后台-04 套餐）、`billing`（后台-05 订单与收款）。
- 目录：`src/admin/screens/{dash,tickets,users,plans,billing}/`、`dev/mock/admin/{同名}.ts`。
- 后台外框（`src/admin/` 下的 Shell、Sidebar、modules.ts 等）的后续改动归你：外框需要改时，别的会话会经协调会话转给你。
- 开工前提：后台前端二的第 ⓪ 步「接线」已合入主线（页面登记表、子路由、拆分后的假后端、侧栏按权限隐藏）。开工先 `git merge feat/panel-redesign`，读 `src/admin/screens/CLAUDE.md` 与 `dev/mock/CLAUDE.md`。

## 步骤（每步一个提交，做完停下报告）

| 步 | 内容 | 依据 |
|---|---|---|
| ① 仪表盘 | 「需要处理」卡片（与侧栏徽标同一个接口）、经营总览、收入趋势、节点与用户流量排行、通知投递积压、系统状态卡（含待补·前端的第 8 行「数据库备份」与抽屉） | 后台-01；待决 D-A-2 |
| ② 工单 | 队列（筛选、SLA）、详情（对话、内部备注、关联订单、升级、关闭原因）、快捷回复 | 后台-02；待决 D-B-6 |
| ③ 用户列表与详情 | 列表（字段、筛选、用户组列）、用户详情抽屉（完整资料、订阅、订单、设备、余额调整、重置密码、换发订阅链接——**后台看不到订阅地址**，换发后不回明文） | 后台-03；保留规则 2；5.A D-B-1；待决 D-B-2、D-B-8 |
| ④ 用户其余标签 | 用户组（增删改）、批量运营（批量生成、群发、导出）、设备策略、流量重置 | 后台-03；待决 D-B-3、D-B-4、D-B-5、D-B-7 |
| ⑤ 套餐 | 套餐列表、套餐向导（`catalog.publish` + reauth）、版本与价格、高级设置、取消归档、**流量包 tab**（后端有、设计稿没有，按设计稿风格补） | 后台-04；5.A D-C-2；待决 D-C-1、D-C-5、D-E-3 |
| ⑥ 订单与收款 | 订单（列表、详情、人工开单、标记已支付）、挂账（按币种合计）、支付渠道（统计、渠道未启用的处理）、收入调整（登记人、生效日、冲销） | 后台-05；保留规则 6（「欠费单」即「挂账」）；待决 D-C-3 |

后端依赖（写报告时核对主线进度）：
- ① 的 `GET v1/dashboard/tasks` 与仪表盘扩展字段在后端二 ⑤（M9 权限 `ops.dashboard.read`）。
- ② 的快捷回复在后端二 ④（M2），工单字段在后端二 ⑤。
- ③④ 的用户字段在后端二 ⑤。
- ⑤ 的流量包管理接口在后端一 ⑥，契约形状届时以修订给出；在那之前先做 ⑤ 的其余部分，流量包 tab 放到 ⑥ 之后补，或在报告里标为「等后端」。
- ⑥ 的订单扩展、挂账扩展、渠道统计、收入调整登记人都在后端一 ⑥。

## 待决项（按「未决前」处理，碰到时在报告里列出）

D-A-1（外框实时事件，已由第 2 阶段按通用条目处理，不用再动）、D-A-2、D-B-2 至 D-B-8（D-B-1 已决）、D-C-1、D-C-3、D-C-5、D-E-3。

## 进度与补充事项（协调会话维护，接力的新会话从这里接上）

**进度**：① 仪表盘 `f0b97fd`、`da73257`（合并 3b0930e）、② 工单 `a35785e`（合并 415fa3a）、写操作三件提升 `af5158f`（合并 2c1fc82）、③ 用户列表与详情 `9c4a509`（合并 9a45d53；NativeCore 36085029781、手动 PG18 36085060773 全绿）已验收合入。「④ 之前」`b1b9b77`（合并 0e4cbd5）、④ 用户其余标签 `77612c2`（合并 f63b69e；NativeCore 36089193990 第 2 次运行全绿，第 1 次因账号计费未启动）已验收合入。「⑤ 之前」`2a5a00e`（ui/Select `emptyOption`、ui/Field 空串不算错误，合并 9a8693a）已合入。「⑤ 之前（二）」`4263c49`（合并 1ccfd2b）、⑤ 套餐 `0a09ddc`（合并 3601617；NativeCore 36103667921 全绿）已验收合入。**下一步 ⑥ 订单与收款（后台前端一最后一步）。** 2026-09-24 ⑤ 之后上下文用完（第三次接力），同一 worktree 由新会话接力：先读 `phase3-common.md`、本文件全文、`src/admin/CLAUDE.md`、`src/admin/screens/plans/CLAUDE.md`、`dev/mock/admin/CLAUDE.md`，再 `git merge feat/panel-redesign`。

补充事项（与上文冲突时以这里为准）：
- 契约修订已到 R94。
- **后端一已全部完成（2026-09-24，合并 b0c830d）**：⑤ 的流量包 tab 按契约后台-04「后台流量包管理」（R73）做，不用再等；⑥ 的人工开单「线下已收款 + 凭证号」已开放（R74），接口已挂 reauth，凭证号重复的 409 文案是英文，前端映射成「凭证号已用于其他订单」。
- `GET v1/me` 的 `email`、`display_name`、`roles` 是待补·后端字段，外框已按可选处理；后端补上后自动生效，不用改。
- 只读演示账号 `viewer@pandora.dev`（后台）用于实测按权限隐藏。
- **② 之前先做（单独一个提交，协调会话会尽快合并，门户会话在等）**：
  1. 把 `formatBytes`（BigInt 安全）、`formatDateTime`、`formatCount` 从 `dash/model.ts` 提升到 `src/core/format.ts`，补单元测试，仪表盘改为从 core 引用；
  2. 把 `GET v1/dashboard/tasks` 的 schema 与 hook 提到 `src/admin/` 这一层（如 `src/admin/tasks.ts`），侧栏徽标与仪表盘共用同一个查询键和同一份严格 schema（待补字段按可选）。
- 仪表盘按契约修订 R52 调整：系统状态组件的 `metrics` 各字段改为可选，缺失显示「—」（可并进上面那个提交）。
- 节点排行的协议后缀先不做（契约允许取不到不显示）；节点列表的查询键由后台前端二做节点页时定，届时协调会话再通知。
- KPI 格子分隔线改用每格右、下 1px 阴影，认可。
- 后端二已全部完成（R53–R62），仪表盘的待补接口（tasks、overview 字段、previous_total、components、active_users）都已上线，接下来的步骤里顺带在假后端上对齐最终形状即可；工单队列的新字段见 R60。
- 后端一 ⑥ 第一部分已上线（R63–R70）：订单列表多值状态与渠道、人工开单 `settlement: grant | pending`（`offline` 待该接口加 reauth 后开放，R64）、套餐向导响应改为建成后详情（R65）、渠道卡今日成交与成功率（R66）。
- **后台窄屏已定（用户 2026-09-24）**：后台只支持 960 及以上，窄于 960 允许横向滚动，不做抽屉式侧栏，外框不用改；各步只验 1280 与 960。
- （已合入，见文末）后台前端二把四格统计条、分页、「加载 / 错误 / 空」三态容器提升到 `src/ui/`；② 起的新页面用 ui 版本（合入前可以先在自己目录写，合入后改用），仪表盘已有的不强制改。
- 后台前端二在营销假后端里临时挂了一份 `GET v1/plans`（只含营销用到的字段）；你做 ⑤ 套餐时在 `dev/mock/admin/plans.ts` 实现完整版，登记表里 plans 排在前面会自动接管，告诉协调会话，由后台前端二删掉临时那份。
- **ui 新组件已合入主线（后台前端二 1b1ac26，合并 c93dec6）**：`StatStrip`（四格统计条）、`Pager`（total / limit / offset 分页）、`QueryView`（react-query 结果的加载 / 404 无权限 / 错误重试 / 空 / 正文五态），从 `ui/index.ts` 取。本步新写的页面改用它们，自己目录里的临时版本删掉；`enabled: false` 的查询不要套 `QueryView`（`isPending` 会一直为真）。`Checkbox` 带文字标签时现在用 `htmlFor` 关联，按名字查找能找到。
- ② 的验收结论：关闭说明框、系统消息里状态码换中文叫法、左栏 `min(340px, 38%)`、「超时」缩写、D-B-6 确认框写「目前不会自动通知任何人」，都认可。后台详情不填 `related_order` / `last_reply_at` / `message_count` 已写成契约 R75，后端补齐列入遗留；关联订单的 schema 保留，有值才显示，`#/billing/orders/<id>` 深链在 ⑥ 实现。
- **（已完成，af5158f）③ 之前先做**：把 `tickets/actions.ts` 的 `useCan` / `useIntentKey` / `useFailure` 提升到新文件 `src/admin/actions.ts`（后台外框归你，新增文件），补单元测试，工单改为从那里引用、删掉 `tickets/actions.ts`。营销里同名的三件由后台前端二在合并后自己切换，你不要改 marketing 目录。
- ③ 起的新页面用 ui 的 `StatStrip` / `Pager` / `QueryView`（左右分栏的详情面板、无限滚动队列这类不合适的场景可以不套，报告里说明）。
- ③ 的验收结论：列表与抽屉状态全在地址上（`#/users/list/<id>/<标签>?f=&g=&q=&o=`）、保留规则 2（订阅标签不显示地址、换发响应用 strict schema 守「不回令牌」）、按权限分层显示操作、表格最小宽 860 在 960 下卡片内横向滚动，都认可。D-B-2 按方案 A（管理员直接设新密码 + 原因）、D-B-8 按方案 A（只显示在线台数与上限）是「未决前」处理，协调会话会汇总给用户定。
- **④ 要点**：用户组增删改（后台-03；D-B-3 未决按「未决前」）、批量运营（批量生成、群发、导出；D-B-4、D-B-5）、设备策略（D-B-7）、流量重置（`POST v1/users/{id}/traffic-reset`（原写 quota/reset 是笔误），按邮箱重置先 `GET v1/users?q=` 取 email 完全相等的那条；两处确认框都要必填「重置原因」5–500 字，接口要 reauth）。碰到的待决项在报告里逐条列出怎么处理的。
- ③ 报告的补充结论：
  - 顺延：抽屉里设计稿的「流量重置」标签与「立即重置本期」放到 ④ 与流量重置页一起做；「为其开单」按钮与订单深链 `#/billing/orders/<id>`、`#/billing/orders?user_id=<id>` 放到 ⑥ 接住。
  - `stats.paid_total` 跨币种相加已写成契约 R80，前端暂按余额币种显示，后端拆币种列入遗留。
  - **（已完成，b1b9b77）④ 之前先做**：① `ui/Table` 支持行级键盘激活（有行点击时行可聚焦、Enter / 空格触发同一回调、带可见焦点样式；不传行点击时行为不变），补 `ui.test.tsx` 结构测试——这是共享层改动，协调会话指派给你；② `tests/mock-api.test.ts` 里调账用例改用 `dev/mock/admin/users.ts` 的真实种子 id 与种子余额，然后删掉 users 假后端里为 `u1` 保留的兼容口径。两件并一个提交。
- ④ 的验收结论：四个标签、抽屉「流量重置」与「立即重置本期」、样式拆出 `Ops.module.css`、假后端拆出 `users-ops.ts`、`tests/mock-api.test.ts` 追加用户 ④ 测试块，都认可。待决 D-B-3（隐藏可用节点池列）、D-B-4（识别窗口只读说明）、D-B-5（去掉全局默认设备数）、D-B-7（批量生成不带开通套餐）按方案 A 认可，协调会话会汇总给用户定。契约：R86。
- **（已完成，2a5a00e）⑤ 之前先做**：① `ui/Select` 支持可选的「不限 / 全部」空值项（占位项目前是 disabled，选过别的值就回不去；加一个显式属性，如 `emptyOption?: string`，传了就渲染一个可选的空值项，不传时行为不变），用户列表的「全部用户组」与 ④ 的筛选改用它、删掉页面里的临时写法；② `ui/Field` 的 `error` 为空串时不算错误（不显示红框与文字），补 `ui.test.tsx`。推送、报告，合并后再做 ⑤。
- ⑤ 套餐：`dev/mock/admin/plans.ts` 实现完整 `GET v1/plans` 时沿用 users 假后端里固定的套餐 id（`9c0e1a2b-3333-…`），批量筛选按套餐就能命中；做完告诉协调会话，由后台前端二删掉营销假后端里临时的那份。⑤ 的流量包 tab 按 R73。
- 「⑤ 之前」的验收结论：`emptyOption` 与 placeholder 分工写进组件头注释、`hasError` 一处判定同时管 Field 错误行与控件的 `aria-invalid`、抽屉用户组下拉一并改用，都认可。
- **（已完成，4263c49）⑤ 之前（二）**：按公共规则 10.4 与契约 R85，`src/admin/actions.ts` 让写操作在收到 4xx 业务拒绝后丢弃幂等键（reauth 取消即 `reauth_required` 除外，断网与 5xx 保留）。做法你定，但要一处集中、调用方改动最小（例如给 `useFailure` 加可选的 intent 参数、或提供 `endsIntent` 并在 `useFailure` 里 reset）；补单测；工单、用户、营销、节点四个目录的调用处都换上。后台前端二这段时间做 ④ 内容与外观，不会碰 marketing / nodes 目录。推送、报告，合并后再做 ⑤。
- 「⑤ 之前（二）」的验收结论：规则只在 `actions.ts` 的 `endsIntent` 一处（4xx 丢键、`reauth_required` 除外，断网 / 5xx / `invalid_response` 保键），`useFailure` 兼容旧写法、新写法 `fail(e, { intent })`，自己先处理 4xx 的分支在 catch 开头 `if (endsIntent(e)) intent.reset()`，工单「回复并解决」只丢解决那把键，都认可。之后新写的带幂等键的写操作一律按这个写法。
- 浏览器实测的登录方式：不要在登录页或 reauth 框里手输口令。照其他会话的做法，用命令行向本机假后端取演示令牌（`dev/mock-api.ts` 的 `MOCK_ACCOUNTS` 是本地夹具，不是真实凭据）再写进浏览器存储；reauth 过期后用同样方式刷新 rat，再提交。
- ⑤ 的验收结论：套餐标签（卡片 + 详情、价格 / 线路 / 版本卡、五步向导、销售设置抽屉、归档）、流量包标签（R73，updated_at 乐观锁）、`plans/failure.ts`（503 说明销售开关、422 无表单时 Toast 字段原因、409 原文）、960 下版本行用 container query、步骤栏只给通过校验的步打 ✓，都认可。价格卡与线路卡最小宽 340 → 320（避免随滚动条跳列）认可。待决 D-C-1（不做恢复上架）、D-C-5（向导不放限速，版本里仅「用完限速」可填）、D-E-3 按「未决前」认可。编辑向导三处后端现状写成 R92，列入后端遗留。
- `GET v1/plans` 目前有用户页、营销页、套餐页三个查询键：先不合并（数据互不污染，只多两次请求），第 3 阶段收尾时再看。
- **⑥ 要点**：订单（列表多值状态与渠道 R63、详情、人工开单 `settlement` grant / pending / offline（R64、R74，offline 必带凭证号、接口要 reauth；凭证号重复 409 文案是英文，映射成「凭证号已用于其他订单」；`balance` 按 D-C-3 未决，不提供）、标记已支付）、挂账（保留规则 6，按币种合计 R3）、支付渠道（今日成交与成功率 R66、渠道未启用的处理）、收入调整（登记人、生效日、冲销 R66）；人工开单弹窗的套餐下拉复用 `plans/api.ts`；接住 ③ 留下的深链 `#/billing/orders/<id>`、`#/billing/orders?user_id=<id>`，以及用户抽屉的「为其开单」。
- ⑥ 的假后端测试不要再往 `tests/mock-api.test.ts` 里加（已超 800 行，后台前端二正在按模块拆开）：新建 `tests/mock-admin-billing.test.ts`，需要的 `serve` 与登录辅助先在新文件里写一份，头注释写明「待 tests 拆分合入后改用 `tests/mock-helpers.ts`」。
