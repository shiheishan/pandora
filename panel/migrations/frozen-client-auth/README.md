# 冻结的客户端认证迁移

这两个迁移属于 CA42 客户端认证子系统。项目当前只做面板，不做客户端，
该子系统已冻结，所以它们被移出主迁移序列 —— 主序列要求编号严格连续，
留在原地会让之后每一个迁移都过不了 deploy/check-migrations.sh 的预检查。

它们从未在任何环境应用过（生产当时停在 40），因此移走不影响任何已有数据库。

要恢复 CA42 时：把文件移回 migrations/ 并按当时的最大编号重新排号，
不要沿用 00042/00043 —— 这两个号已经被 00042_seed_registration_mode.sql 占用。
