-- +goose Up
-- 删掉上一版加的同源聚类索引。
--
-- 加它的理由是「让 90 天窗口能走索引裁剪」，听起来合理，实测是错的：
-- 规划器始终选 idx_audit_events_time 做 Bitmap Index Scan，
-- 因为聚类查询要读窗口内的全部行，位图扫描本来就比这条复合索引更划算。
--
-- 30 万行规模下实测（同一临时库、同一数据）：
--
--   查询       有索引 183ms   无索引 166ms
--   写 3 万行  有索引 4142ms  无索引 1865ms
--   索引占用   32 MB
--
-- 查询没变快，写入慢了一倍多。审计表在每个业务操作的事务里都要写一条，
-- 是不折不扣的热路径 —— 为一个用不上的索引给它加一倍写开销，
-- 等于让所有下单和登录替一个空想的优化买单。
--
-- 留下这条迁移而不是回头改 00026：迁移一旦应用就是既成事实，
-- 改它会让已经跑过的环境和文件对不上。写清楚为什么删，比抹掉痕迹有用。

DROP INDEX IF EXISTS audit_events_cluster_idx;
